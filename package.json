{
  "name": "north-star-3d",
  "version": "1.0.0",
  "description": "Why the North Star doesn't move — a 3D sky & solar-system explorer",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "test": "node --test"
  }
}
